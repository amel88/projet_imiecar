{% extends 'base.html.twig' %}

{% block body %}

    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}"/>
    <div class="container">
        <div class="row">

            <div style="margin-bottom: 25px" id="trajet" class="col-sm-12">
                <div id="entete"><h1><strong>Liste des commentaires</strong></h1></div>
            </div>

            <div style="padding-top: 35px" id="trajet" class="col-sm-12">
                <div class="row">
                    <div style=" color:white;text-align: center; " class="col-sm-12">
                        <div class="trou"></div>

                        {% for commentaire in commentaires %}

                            <div class="row fondtrajet2 col-sm-12">
                                <div style="text-align: left" class="col-sm-6">
                                    <p>Posté le <strong><span>
                                    {{ commentaire.date|date('d-m-Y H:i') }}</span></strong> Par <strong><span>{{ commentaire.idUsers }}</span></strong>  </p>
                                </div>

                                <div style="text-align: left" class="col-sm-6">
                                    <p>Avis sur <strong><span>{{ commentaire.idUsers2 }}</span></strong></P>
                                </div>
                            </div>

                            <div class="row fondtrajet col-sm-12">
                                <div style="text-align: center" class="col-sm-3">
                                    <span>{{ commentaire.text }}</span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-12">
                                    <br>
                                </div>
                            </div>

                        {% endfor %}
                        <div class="row col-sm-12">
                            <div class="col-sm-12">
                                <br>
                                <div style="text-align: center" class="col-sm-12">
                                    <button class="btn-button-right"
                                            onclick="location.href= '{{ path('commentaires_new') }}' ">{{ 'Ecrire un nouveau commentaire' }}</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <br><br>
            </div>
        </div>
    </div>
{% endblock %}
